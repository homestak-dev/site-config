# Edge VM specification
# Simple Debian endpoint: packages, user with sudo, timezone
---
schema_version: 1

access:
  posture: dev
  users:
    - name: homestak
      sudo: true
      ssh_keys:
        - ssh_keys.jderose@father

platform:
  packages:
    - htop
    - curl

config:
  timezone: America/Denver
