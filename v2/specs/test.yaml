# Test VM specification
# Used by spec-vm-roundtrip scenario to validate Create → Specify flow
---
schema_version: 1

# Identity is set from HOMESTAK_IDENTITY env var at runtime
# identity: test

# Use dev posture (network trust, no auth token required)
access:
  posture: dev

platform:
  packages:
    - curl
    - jq
