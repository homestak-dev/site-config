# Pull mode integration test specification
# Used by spec-vm-pull-roundtrip scenario (iac-driver#156)
---
schema_version: 1

access:
  posture: dev
  users:
    - name: homestak
      sudo: true
      ssh_keys:
        - ssh_keys.jderose

platform:
  packages:
    - htop
    - curl

config:
  timezone: America/Denver
