# PVE-Deb environment (nested PVE for E2E testing)
# Primary key derived from filename: pve-deb.yaml -> pve-deb
# Node specified at deploy time via: tofu apply -var="node=<name>"
# Full topology definition added in Phase 5
