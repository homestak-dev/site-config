# Physical host: father
# Primary key derived from filename: father.yaml -> father
# Rich config (network, storage) added in Phase 4

access:
  ssh_user: jderose
  authorized_keys:
    - jderose           # FK -> secrets.ssh_keys.jderose
