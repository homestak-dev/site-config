# Physical host: pve (generic/alias)
# Primary key derived from filename: pve.yaml -> pve
# Rich config (network, storage) added in Phase 4

access:
  ssh_user: root
  authorized_keys:
    - jderose           # FK -> secrets.ssh_keys.jderose
