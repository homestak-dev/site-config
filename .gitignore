# Plaintext secrets (never commit)
*.tfvars
*.yaml
*.json

# Except templates and sops config
!*.tpl
!.sops.yaml

# Encrypted secrets (gitignored by default for public template)
# Remove these lines if you want to commit encrypted secrets to a private fork
*.tfvars.enc
*.yaml.enc
*.json.enc

# Editor files
*.swp
*~
.idea/
.vscode/

# OS files
.DS_Store
