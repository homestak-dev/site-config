# Secrets (plaintext and encrypted are both local-only)
# Use secrets.yaml.example as a starting template
secrets.yaml
secrets.yaml.enc

# Site-specific configs (generated per-deployment via make host-config, make node-config)
hosts/*.yaml
nodes/*.yaml
# Keep nested-pve.yaml tracked for integration testing
!nodes/nested-pve.yaml

# Editor files
*.swp
*~
.idea/
.vscode/

# OS files
.DS_Store
