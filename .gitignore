# Plaintext secrets (never commit)
secrets.yaml

# Site-specific node configs (generated per-deployment via make node-config)
# Keep nested-pve.yaml tracked for integration testing
nodes/*.yaml
!nodes/nested-pve.yaml

# Encrypted secrets (committed to enable sharing with private forks)
# The plaintext secrets.yaml is still gitignored above

# Editor files
*.swp
*~
.idea/
.vscode/

# OS files
.DS_Store
